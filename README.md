# c-text-editor-undo-redo
Menu-driven C text editor (word-based) that loads text from a file, supports insert/remove operations, and implements Undo/Redo using stacks (plus a queue for sentence processing), then saves results to output.txt.
